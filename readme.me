Simple Express Proxy Server with CORS headers for binance

Cone the repository:
```https://github.com/mkruneva/express-cors-proxy.git```

Install dependencies: 
```npm i```

Start the server:
```npm start```

The binance proxy on port :8090
